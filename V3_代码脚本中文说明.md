# V3 代码脚本中文说明

适用代码包：`parallel_at_braak_evidence_V3_code.zip`。整理日期：2026-09-18。

代码包共有 16 个 Python 脚本、7 个 R 脚本，以及项目根目录下的 1 个 Bash 运行脚本。所有 Python 和 R 脚本均位于一级 `scripts` 目录。

这些代码负责数据整理、统计分析和论文结果表汇总，不直接生成投稿 Word，也不包含全部投稿图片的绘图程序。下文所列输出为主要输出，不是每个脚本全部字段的清单。

## 一、最常用的三个入口

| 文件 | 主要作用 | 什么时候用 |
|---|---|---|
| `run_analysis.sh` | 按默认设置启动完整分析，并使用带时间戳的新输出目录 | Linux 下希望一次运行全部分析时 |
| `scripts/run_paper.py` | 完整流程的 Python 总入口，可指定数据路径、R 路径和输出目录 | 需要自行设置运行参数时 |
| `scripts/collect_paper_results.py` | 把已完成分析的论文来源表集中到一个目录，并生成 ZIP | 分析已经完成，只想重新汇总结果时 |

正常运行完整流程时，不必逐个手动执行所有脚本。`run_paper.py` 会调用原始分析、补充分析和结果汇总。

## 二、执行顺序

```text
run_analysis.sh
└── run_paper.py
    ├── prepare_adni.py
    ├── analyze_adni.py
    │   └── adni_models.py（内部模型函数）
    ├── prepare_gse131617.R
    ├── analyze_gse131617.R
    ├── prepare_pathway_expression.R
    ├── analyze_pathways.R
    ├── summarize_uty_sensitivity.R
    ├── compile_results_tables.py
    │   └── go_submission_policy.py（内部筛选函数）
    ├── run_additional.py
    │   ├── cross_sectional.py
    │   ├── tau_sensitivity.py
    │   ├── spline_stability.py
    │   ├── longitudinal.py
    │   ├── clinical_tables.py
    │   ├── longitudinal_sensitivity.py
    │   ├── selection_sensitivity.py
    │   └── stage_sensitivity.py
    └── collect_paper_results.py
```

`install_r_packages.R` 和 `check_r_environment.R` 为环境准备工具，不在上面的常规分析顺序中。需要安装或核对精确 R 版本时单独使用。

## 三、运行与环境脚本

### 1. `run_analysis.sh`

- **作用**：Linux/Bash 一键运行入口。先进行输入检查，再启动完整分析。
- **设置**：默认参与者 bootstrap 2,000 次、随访加权 bootstrap 1,000 次，随机种子为 `20260915`。
- **输出位置**：`results/paper_run_日期_时间/`；其中 `paper_results.zip` 为汇总结果包。
- **注意**：默认 Python 为 `python3`；Rscript 使用脚本中设置的路径。可通过 `PYTHON`、`RSCRIPT` 环境变量替换。

### 2. `scripts/run_paper.py`

- **作用**：组织整个分析流程，本身不拟合统计模型。
- **输入**：项目配置、ADNI 原始表、GSE131617 数据、基因和 GO 字典。
- **工作内容**：检查输入和运行环境，为本次运行设置独立的数据及结果路径，依次调用各分析脚本，最后汇总结果。
- **输出**：本次运行目录中的 `config/`、`data/`、`results/`、`paper_results/` 和结果 ZIP。
- **注意**：完整运行目录包含受限的 ADNI 输入或衍生数据，不能把整个目录直接上传 GitHub。`--dry-run` 不运行模型，也不完成 R 包检查。

### 3. `scripts/run_additional.py`

- **作用**：统一运行八项补充临床分析。
- **输入**：已生成的 ADNI 对齐主表，以及 CDR、诊断、受试者和下载日期等数据。
- **输出**：默认在 `results/extensions/` 下按分析名称分目录保存结果。
- **注意**：它不负责从原始 PET 数据开始建表，也不运行 GSE131617 分析。

### 4. `scripts/install_r_packages.R`

- **作用**：在 R 4.4.2 环境中安装 Bioconductor 3.20 系列核心包及相关 R 包。
- **内容**：安装 limma、注释数据库、yaml、readxl、DBI、RSQLite 等，随后调用环境检查脚本。
- **注意**：该脚本会改变 R 环境，并需要联网；分析已经正常运行时，不需要每次重新安装。

### 5. `scripts/check_r_environment.R`

- **作用**：打印 R 和相关包的版本，并检查核心版本是否符合代码规定。
- **输出**：终端中的版本列表；缺包或核心版本不匹配时停止。
- **注意**：仅检查环境，不读取研究数据、不拟合模型。完整运行入口中的包可加载检查不能替代这里的精确版本检查。

## 四、ADNI 原始临床与影像分析

### 6. `scripts/prepare_adni.py`

- **作用**：将 ADNI 多张原始数据表整理为 PET 时间对齐的参与者级主表。
- **输入**：淀粉样 PET、tau PET、MRI、CDR、APOE、人口学和 FDG 等数据及配置。
- **工作内容**：按代码定义选择和对齐记录，整理 A/T 分组、结局及协变量。
- **主要输出**：`adni_pet_aligned_master.csv`。
- **注意**：主表是参与者级数据，不是可直接公开的论文汇总表。

### 7. `scripts/adni_models.py`

- **作用**：提供 ADNI 分析共用的回归和统计计算函数，供 `analyze_adni.py` 调用。
- **输入**：调用脚本传入的数据、模型和对比设置。
- **输出**：返回模型估计及相关统计结果，由调用脚本保存。
- **注意**：这是内部模块，通常不需要单独运行，也不能在保留分析入口的同时删除它。

### 8. `scripts/analyze_adni.py`

- **作用**：拟合原始 ADNI 横断面模型及相应敏感性分析。
- **分析内容**：A/T 与海马体积/ICV、CDR-SB 的关联，FDG 探索分析、APOE 交互和分层，以及时间窗、影响点和其他稳健性分析。
- **输入**：对齐后的 ADNI 主表和配置。
- **主要输出**：`adni_results.csv`、`adni_model_counts.csv`。
- **论文用途**：原始横断面主表、相关敏感性表及 A/T、FDG、APOE 图的数据来源。

## 五、补充临床分析

### 9. `scripts/cross_sectional.py`

- **作用**：补充直接 A+T+ 对 A+T− 的比较，以及 A+ 人群内连续 tau 与结局的关联。
- **分析内容**：海马体积/ICV、CDR-SB，全部合格年龄样本及 ≥65 岁样本，探索性 FDR 校正。
- **输入**：ADNI 对齐主表。
- **主要输出**：`extension_results.csv`、年龄覆盖及基线描述结果。
- **注意**：这些结果属于探索性分析，与原始验证性检验分开解释。

### 10. `scripts/tau_sensitivity.py`

- **作用**：检查连续 tau 关联是否存在非线性，以及是否受高影响观测影响。
- **分析内容**：自然三次样条、非线性联合检验、影响点识别及排除后的线性模型比较。
- **输入**：ADNI 对齐主表；调用补充横断面分析中的共用函数。
- **主要输出**：`nonlinearity_results.csv`、`influence_sensitivity.csv`、`adjusted_tau_curves.csv`。

### 11. `scripts/spline_stability.py`

- **作用**：比较排除高影响观测前后的样条曲线形状。
- **方法特点**：沿用全样本的结点和基函数设置，在共同 tau 范围及相同参考点下比较，避免把重新设定结点造成的变化误当作曲线差异。
- **主要输出**：`spline_stability_summary.csv`、`spline_common_range_comparison.csv`。

### 12. `scripts/longitudinal.py`

- **作用**：构建 A+ 纵向队列，分析基线 tau 与后续 CDR-SB 变化的关联。
- **分析内容**：主 GEE 模型，以及 PET 前基线、90 天窗口、至少两次随访等敏感性分析。
- **输入**：ADNI 主表、CDR 随访和诊断数据。
- **主要输出**：`longitudinal_results.csv`、`model_coefficients.csv`、`sample_flow.csv`、`followup_coverage.csv`、`attrition_comparison.csv`、`diagnosis_at_tau.csv`。
- **其他用途**：向多个补充脚本提供读表、日期处理、队列构建等函数。

### 13. `scripts/clinical_tables.py`

- **作用**：生成临床基线特征表、纵向队列描述，以及有无合格随访者的比较。
- **输入**：ADNI 主表及相关临床记录。
- **主要输出**：`table1a_clinical_by_at.csv`、`table1b_longitudinal_cohorts.csv`、`table_s9_followup_comparison.csv`，以及未格式化数值、诊断构成和缺失情况表。
- **论文用途**：主文 Table 1A、1B 和补充表 S9。
- **注意**：描述性统计按各变量的有效观测计算，不能把所有变量的分母都当成同一个完整病例样本量。

### 14. `scripts/longitudinal_sensitivity.py`

- **作用**：检查纵向结果对参与者影响、重抽样、随访机会和结局模型形式的敏感程度。
- **分析内容**：逐人删除、参与者 bootstrap、日历随访机会、二次时间项、有界结局模型，以及不同时间点的标准化变化对比。
- **主要输出**：`cluster_resampling_summary.csv`、`calendar_opportunity_sensitivity.csv`、`calendar_opportunity_summary.csv`、`standardized_change_contrasts.csv`、`time_bin_calibration.csv` 等。
- **注意**：日历机会分析与随访选择加权分析不是同一件事；部分标准化对比是累计变化差异，不是固定年斜率。

### 15. `scripts/selection_sensitivity.py`

- **作用**：考察有无合格随访的选择差异对纵向关联估计的影响。
- **分析内容**：拟合随访可获得性模型，计算逆概率权重及 1%–99% 截尾权重，进行加权 GEE、平衡度比较和全流程 bootstrap；另输出临床分期调整结果。
- **主要输出**：`selection_sensitivity_results.csv`、`weight_diagnostics.csv`、`balance_against_eligible.csv`、`bootstrap_summary.csv`、`selection_model_coefficients.csv`、`diagnosis_counts.csv`。
- **新增输出**：`selection_probability_summary.csv`，分别汇总全部合格者、有随访者和无随访者的概率范围，不输出个体概率。
- **注意**：GEE 置信区间与重新估计权重的全流程 bootstrap 置信区间含义不同，使用时应按论文对应方法选取。

### 16. `scripts/stage_sensitivity.py`

- **作用**：比较临床分期调整及结局模型形式对纵向 tau 关联的影响。
- **分析内容**：Gaussian 与 fractional-logit 模型、临床阶段调整、一年及两年的标准化变化对比、分阶段校准和 tau 分布支持情况。
- **主要输出**：`stage_bounded_models.csv`、`standardized_stage_change.csv`、`stage_model_comparison.csv`、`stage_model_coefficients.csv`、`stage_time_calibration.csv`、`tau_support_by_diagnosis.csv`、`sample_flow.csv`。
- **论文用途**：临床分期敏感性分析、补充表 S12–S14，以及主图 Figure 4 的相关数据。

## 六、GSE131617 转录组与通路分析

### 17. `scripts/prepare_gse131617.R`

- **作用**：整理 GEO 表达数据、平台注释及供者信息。
- **输入**：GSE131617 series matrix、供者 Excel 文件和平台注释包。
- **主要输出**：`expression_feature.csv.gz`、`feature_annotation.csv`、`sample_manifest.csv`。
- **注意**：这是对所提供表达矩阵的整理和映射，不应描述为从原始芯片 CEL 文件重新完成全部预处理。

### 18. `scripts/analyze_gse131617.R`

- **作用**：使用供者分块的 limma 模型，分析晚期 Braak V–VI 相对 Braak 0 的差异表达。
- **分析内容**：脑区及协变量调整、供者内相关性、经验贝叶斯估计、FDR 校正、逐脑区删除及方向一致性筛选。
- **输入**：特征级表达矩阵、注释和样本清单。
- **主要输出**：`braak_feature_results.csv`、`braak_region_sensitivity.csv`、`braak_representative_genes.csv`、`braak_robust_genes.csv`。
- **论文用途**：差异表达主表、完整补充表 S2、17 个基因结果和相关表达图。

### 19. `scripts/prepare_pathway_expression.R`

- **作用**：将特征级表达数据整理为用于通路分析的 Entrez 基因级矩阵。
- **方法**：从平台 SQLite 注释中筛选唯一映射；一个基因对应多个合格特征时，按平均表达量及特征编号规则选择代表特征。
- **主要输出**：`pathway_expression.csv.gz`。
- **注意**：它负责通路分析输入的整理，不计算 GO 显著性。

### 20. `scripts/analyze_pathways.R`

- **作用**：利用基因排名统计量进行 GO 生物过程通路分析，并检查逐脑区删除后的方向一致性。
- **输入**：Entrez 级表达矩阵、样本清单、GO 注释及配置。
- **主要输出**：`ranked_pathways.csv`、`stable_pathways.csv`。
- **报告规则**：筛选满足统计和方向条件的条目，排除废弃条目 `GO:0090309`；排除报告条目不重新计算原检验集合的 FDR。
- **注意**：通路结果是组织层面的关联，不是细胞类型或机制验证。

### 21. `scripts/summarize_uty_sensitivity.R`

- **作用**：汇总各 Braak 阶段的供者性别构成，并标识基因清单中的 UTY。
- **输入**：样本清单、入选基因结果和基因符号字典。
- **主要输出**：`donor_sex_by_braak.csv`、`uty_sensitivity.csv`。
- **关键区别**：这里只展示排除 UTY 后保留哪些基因，不重新拟合差异表达模型，也不是男性供者亚组分析。

### 22. `scripts/go_submission_policy.py`

- **作用**：向 Python 汇总程序提供统一的 GO 条目筛选函数。
- **规则**：根据全模型 FDR 和方向一致性选择条目，并排除 `GO:0090309`，不修改已有 P 值或 q 值。
- **输出**：返回筛选后的数据表，由调用脚本保存。
- **注意**：这是内部模块，通常不单独运行。

## 七、论文结果整理脚本

### 23. `scripts/compile_results_tables.py`

- **作用**：将已完成的 ADNI、差异表达和 GO 结果整理为紧凑的论文来源表，补充基因符号及 GO 名称。
- **输入**：模型结果、样本量表、基因结果、通路结果及字典。
- **输出**：`manuscript_tables/` 中的来源 CSV。
- **临床专用入口**：使用 `--clinical-only` 时，调用 `clinical_tables.py` 生成 Table 1A、1B 和 S9 等临床描述结果。
- **注意**：默认模式不重新拟合模型。来源文件中沿用的 `table1`–`table5` 文件名不一定与最终论文表号逐一相同，不能只根据文件名判断论文位置。

### 24. `scripts/collect_paper_results.py`

- **作用**：收集一次完整运行中与论文相关的结果文件，生成统一的结果目录和 ZIP。
- **工作内容**：
  - 按明确清单收集主表、补充表和图的数据来源。
  - 将特征级模型结果、逐脑区结果和注释按特征编号合并为完整 S2。
  - 从本次运行的样本清单汇总供者特征及年龄均值、样本标准差。
  - 生成 A−T+ 描述性汇总。
  - 检查缺失文件及显式参与者标识符列。
- **主要输出**：`source_tables/`、`paper_table_sources.csv`、`README.txt` 和 `paper_results.zip`。当前清单包含 59 个来源 CSV。
- **注意**：不重新拟合模型，但会计算部分描述性汇总。需要完整运行目录中的主表、样本清单和结果；不能仅凭已经导出的 `paper_results` 目录再次执行全部汇总。
- **打包方式**：ZIP 内使用本次运行名称组成的独立顶层目录；已有非空输出目录不会直接覆盖。
- **共享范围**：标识符列检查不等同于全面隐私审查，公开前仍需确认文件符合数据使用协议。

## 八、最简使用方法

在现有 V3 项目根目录，保留原始数据和 `data/contracts` 字典文件：

```bash
conda activate adbio
bash run_analysis.sh
```

如只需重新汇总一次已完成的运行：

```bash
python scripts/collect_paper_results.py \
  --run-dir results/你的完整运行目录 \
  --output-dir results/你的完整运行目录/paper_results_new
```

`--run-dir` 应指向同时包含 `data/` 和 `results/` 的完整运行目录，不是解压后的结果分享包。输出目录及同名 ZIP 应尚不存在或满足脚本的空目录要求。

## 九、其他文件

| 文件 | 用途 |
|---|---|
| `requirements.txt` | Python 第三方依赖及版本范围 |
| `config/project.example.yaml` | 数据位置、分析参数和 Rscript 设置的配置模板 |

这些文件不是统计脚本，但完整运行需要相应依赖与配置。当前代码更新包不包含原始数据及原项目字典，需要与现有 V3 项目配合使用。
